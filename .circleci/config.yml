  
version: 1.0



jobs:
  test:
     docker:
      - image: circleci/openjdk:stretch
      steps:
       -run: mvn clean

  build:
     docker:
      - image: circleci/openjdk:stretch
      steps:
         -run: mvn install
workflows:
  version: 1

  build-then-test:
    jobs:
      - build
      - test:
          requires:
            - build
