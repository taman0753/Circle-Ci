  
version: 2.0

orbs:
  maven: circleci/maven@0.0.12

jobs:
  test:
     machine: # executor type
      image: ubuntu-1604:201903-01
      steps:
       -run: mvn clean

  build:
     machine: # executor type
      image: ubuntu-1604:201903-01
      steps:
         -run: mvn install
workflows:
  version: 2

  build-then-test:
    jobs:
      - build
      - test:
          requires:
            - build
