  
version: 2.0



jobs:
  test:
     docker:
      - image: circleci/openjdk:stretch
      steps:
       -run: mvn clean

  build:
     docker:
      - image: circleci/openjdk:stretch
      steps:
         -run: mvn install
workflows:
  version: 2

  build-then-test:
    jobs:
      - build
      - test:
          requires:
            - build
